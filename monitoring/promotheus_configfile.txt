- job_name: 'blackbox'
    metrics_path: /probe
    params:
      module: [http_2xx]  # Look for a HTTP 200 response.
    static_configs:
      - targets:
          - http://prometheus.io      # Target to probe with HTTP.
          - http://:3000 # Replace with your app's URL/host.
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: 43.204.227.192:9090/:9115  # Replace with your blackbox-exporter hostname:port.


-- job_name: node_exporter
  static_configs:
     - targets:
          - 'YOUR_NODE_IP:9100'  # Replace with real node exporter target(s)
